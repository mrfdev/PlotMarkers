# Paper 26.2 rollback snapshot

Preserved on 2026-09-25 before the Paper 26.3 upgrade, with annotated tag
`v2.0.3-paper-26.2`. Release: PlotMarkers 2.0.3, build 031, Java 25 bytecode,
Paper API `io.papermc.paper:paper-api:26.2.build.84-stable`.

The snapshot includes the outstanding PlotSquared 7.6.0 compile dependency and
the installation, integration, and troubleshooting notes for namespaced worlds.
Plugin identity remains `PlotMarkers`, main class
`com.bobcat00.plotmarkers.PlotMarkers`.

`./scripts/rebuild.sh` passed on JDK 25.0.4.1 on 2026-09-25, including clean
build, release metadata, bytecode, artifact, and documentation checks. Unit tests
reported `NO-SOURCE`. The ignored log is
`servers/upgrade-032/baseline-build.log`; both build 031 artifacts are retained
in `servers/upgrade-032/baseline-artifacts/`.

The existing 2026-09-15 runtime evidence in [the historical testing record](2.0.3-build-031-tests.md) covers
Paper `26.2-84-main@26e81c4` on Java 25.0.4.1 and 26.0.2.1, shaded and thin jars,
361 POI markers, 114 shape markers, BlueMap reload, and clean shutdown. No
Paper 26.3 testing is claimed for this snapshot.

Rollback instances remain ignored and local:

- `servers/Paper-26.2/`: original configured instance, build 84 launcher jar,
  separately staged build 121, and its existing build 030 plugin jar.
- `servers/jdk-refresh-031/runtime/`: build 031 smoke-test fixture.
- `servers/jdk-refresh-031/`: historical logs, marker JSON, and summaries.

The preserved directories are not started or modified by the 26.3 upgrade.
